version: '3.8'

services:
  mongo:
    image: mongo:latest
    container_name: yazlab-mongo
    ports:
      - "27017:27017"
    volumes:
      - yazlab_mongo_data:/data/db

volumes:
  yazlab_mongo_data: